# Clinic Management | Testing Software subject
## Requirement
- MySql >= 8.0
- JDK >= 21
## Start project
- Run all sql script in `/sql` folder
- Add `db.properties` file into `resource` base on `db.pattern.properties`